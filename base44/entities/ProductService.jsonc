{
  "name": "ProductService",
  "type": "object",
  "properties": {
    "tenant_id": {
      "type": "string",
      "description": "Reference to Tenant (isolation)"
    },
    "name": {
      "type": "string",
      "description": "Service name"
    },
    "description": {
      "type": "string"
    },
    "visit_type": {
      "type": "string",
      "enum": [
        "check-in",
        "followup",
        "pre_storm",
        "post_storm",
        "arrival_departure",
        "access_visit",
        "emergency_visit",
        "damage_recovery",
        "auto_care",
        "client_service",
        "concierge"
      ],
      "description": "Associated visit type"
    },
    "type": {
      "type": "string",
      "enum": [
        "addon"
      ],
      "description": "Type (always addon for per-visit billing)"
    },
    "base_price": {
      "type": "number",
      "description": "Price per visit"
    },
    "add_on_charges": {
      "type": "object",
      "description": "Extra charges (e.g., per extra bedroom)"
    },
    "sort_order": {
      "type": "number",
      "default": 0,
      "description": "Display order"
    },
    "is_active": {
      "type": "boolean",
      "default": true
    },
    "is_standard_template": {
      "type": "boolean",
      "default": false,
      "description": "Marks this product as a standard template to be copied to new tenants"
    }
  },
  "required": [
    "tenant_id",
    "name",
    "visit_type",
    "type",
    "base_price"
  ],
  "rls": {
    "read": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "create": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "update": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "delete": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    }
  }
}
{
  "name": "Campaign",
  "type": "object",
  "properties": {
    "tenant_id": {
      "type": "string",
      "description": "Reference to Tenant (tenant isolation)"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "email",
        "sms",
        "both"
      ],
      "description": "Type of campaign"
    },
    "template_id": {
      "type": "string",
      "description": "Reference to CommunicationTemplate"
    },
    "template_name": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "scheduled",
        "sent",
        "cancelled"
      ],
      "default": "draft",
      "description": "Campaign status"
    },
    "scheduled_date": {
      "type": "string",
      "format": "date-time",
      "description": "When to send the campaign"
    },
    "recipient_type": {
      "type": "string",
      "enum": [
        "all_clients",
        "selected_clients",
        "by_property_type"
      ],
      "description": "Who receives this campaign"
    },
    "recipient_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Client IDs to send to (if selected_clients)"
    },
    "property_type_filter": {
      "type": "string",
      "description": "Property type filter (if by_property_type)"
    },
    "sent_count": {
      "type": "integer",
      "default": 0
    },
    "failed_count": {
      "type": "integer",
      "default": 0
    },
    "created_by": {
      "type": "string",
      "description": "User email who created the campaign"
    },
    "sent_at": {
      "type": "string",
      "format": "date-time"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "tenant_id",
    "name",
    "type",
    "template_id",
    "recipient_type"
  ],
  "rls": {
    "create": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "read": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "update": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "delete": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    },
    "write": {
      "data.tenant_id": "{{user.data.primary_tenant_id}}"
    }
  }
}